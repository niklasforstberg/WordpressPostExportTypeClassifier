# Cloudflare Tunnel terminerar TLS, så Caddy svarar på http:// — inget cert här.
# Autentiseringen ligger i Cloudflare Access på hostnamnet, inte i Caddy.
http://posts.forstberg.net {
	import logga posts
	reverse_proxy localhost:5102
}
